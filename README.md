These are the required files to build the python library to use in ROBOT API. These files may be modified in order to fit our needs within ensemble projects

Command to build the lib: 
    `python3 btfpymake.py build`